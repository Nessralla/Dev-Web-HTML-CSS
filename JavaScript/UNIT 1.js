//  Três maneiras de de utilizar o javascript:
//      - dentro do html, utilizando a tag <script type="text/javascript"></script>
//      - em head, inserir script: <script src="arquivo.js" defer></script>
//      - Ou ir em inspecionar e no console, normalmente é utilizado para testes

var a = 1
var b = a + 1
alert(b)

// As variáveis em Javascript podem ter diferentes tipos de dados: Number, String, Boolean, Object, Function
// Undefined (valor indefinido, ou seja, não foi inicializada) e Null (valor nulo)

// typeof() retorna o tipo da variável

// Operador de atribuição =

// Operadores aritimeticos: + - / * e % (resto da divisão)

// Operadores de comparação
    // === comparador de igualdade com tipagem
    // == comparador de igualdade sem tipagem
    // > , < , <= , >= (maior, menor, menor igual e maior igual)
    // !== comparador de desigualdade com tipagem
    // != comparador de desiguldade sem tipagem

// Operadores lógicos: && AND e || OR

// Operadores de atribuição com operação: +=, -= , *= , /= (igual ao Python)

